.. _jit:

Backend Selection (JIT)
-------------------------------------

lmlib supports Just-in-Time (JIT) compilation to significanlty increase execution speed. JIT must be installed and explicitely turned on in the code.